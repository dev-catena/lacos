<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    /**
     * Run the migrations.
     */
    public function up(): void
    {
        Schema::table('users', function (Blueprint $table) {
            // Adicionar campo para caminho do certificado .apx
            if (!Schema::hasColumn('users', 'certificate_apx_path')) {
                $table->string('certificate_apx_path', 500)->nullable()->after('certificate_path');
            }
            
            // Adicionar campo para senha do certificado (criptografada)
            // Nota: certificate_password_encrypted já pode existir, verificar antes
            if (!Schema::hasColumn('users', 'certificate_password_encrypted')) {
                $table->text('certificate_password_encrypted')->nullable()->after('certificate_apx_path');
            }
            
            // Adicionar campo para tipo de certificado (apx ou pfx)
            if (!Schema::hasColumn('users', 'certificate_type')) {
                $table->string('certificate_type', 10)->nullable()->after('certificate_password_encrypted')->comment('apx ou pfx');
            }
        });
    }

    /**
     * Reverse the migrations.
     */
    public function down(): void
    {
        Schema::table('users', function (Blueprint $table) {
            if (Schema::hasColumn('users', 'certificate_apx_path')) {
                $table->dropColumn('certificate_apx_path');
            }
            // Não remover certificate_password_encrypted se já existia antes
            if (Schema::hasColumn('users', 'certificate_type')) {
                $table->dropColumn('certificate_type');
            }
        });
    }
};
















