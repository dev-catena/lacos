<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;
use Illuminate\Support\Facades\DB;

return new class extends Migration
{
    /**
     * Run the migrations.
     */
    public function up(): void
    {
        Schema::table('users', function (Blueprint $table) {
            // Adicionar campo CPF se não existir
            if (!Schema::hasColumn('users', 'cpf')) {
                $table->string('cpf', 14)->nullable()->after('email');
            }
        });

        // Criar índice único para CPF quando profile='doctor'
        // Nota: MySQL não suporta índices parciais diretamente, então vamos usar uma abordagem diferente
        // Vamos criar um índice único composto e validar no código
        try {
            DB::statement('CREATE UNIQUE INDEX unique_doctor_cpf ON users(cpf) WHERE profile = "doctor" AND cpf IS NOT NULL');
        } catch (\Exception $e) {
            // Se falhar (MySQL não suporta WHERE em índices), criar índice simples
            // A validação será feita no código
            if (!Schema::hasIndex('users', 'users_cpf_unique')) {
                Schema::table('users', function (Blueprint $table) {
                    // Não criar índice único aqui, vamos validar no código
                });
            }
        }
    }

    /**
     * Reverse the migrations.
     */
    public function down(): void
    {
        Schema::table('users', function (Blueprint $table) {
            if (Schema::hasColumn('users', 'cpf')) {
                $table->dropColumn('cpf');
            }
        });

        // Remover índice se existir
        try {
            DB::statement('DROP INDEX IF EXISTS unique_doctor_cpf ON users');
        } catch (\Exception $e) {
            // Ignorar erro se índice não existir
        }
    }
};
















