"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.findMedController = void 0;
const localAnvisaMed_service_1 = require("./localAnvisaMed.service");
const debug_1 = __importDefault(require("debug"));
const findMedController = (Req, Res) => __awaiter(void 0, void 0, void 0, function* () {
    try {
        const { filter, value } = Req.query;
        const med = yield localAnvisaMed_service_1.findFiltered(filter, value);
        debug_1.default('API: Findmed:')("query: %O", Req.query);
        Res.set('Cache-control', `public, max-age=${60 * 60}`);
        return Res.send({ status: 'ok', rows: med.length, data: med });
    }
    catch (error) {
        console.error(error);
        return Res.status(500).json({ message: error });
    }
});
exports.findMedController = findMedController;
