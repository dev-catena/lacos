"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.findFiltered = void 0;
const FileProvider_1 = require("./providers/FileProvider/FileProvider");
const Mongo_provider_1 = require("./providers/MongoProvider/Mongo.provider");
const findFiltered = (param, value) => new Promise((resolve, reject) => __awaiter(void 0, void 0, void 0, function* () {
    try {
        const resMongo = yield Mongo_provider_1.findFilteredMongo(param, value);
        if (resMongo.length === 0) {
            const resLocal = yield FileProvider_1.findFilteredLocal(param, value);
            resolve(resLocal);
        }
        resolve(resMongo);
    }
    catch (error) {
        reject(error);
    }
}));
exports.findFiltered = findFiltered;
